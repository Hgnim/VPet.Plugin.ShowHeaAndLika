<Window x:Class="VPet.Plugin.ShowHeaAndLika.window.Setting"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VPet.Plugin.ShowHeaAndLika.window"
        xmlns:pu="clr-namespace:Panuon.WPF.UI;assembly=Panuon.WPF.UI"
        xmlns:ll="clr-namespace:LinePutScript.Localization.WPF;assembly=LinePutScript.Localization.WPF"
        mc:Ignorable="d"
        WindowState="Normal" SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen" FontSize="16"
        Background="{DynamicResource PrimaryLighter}"
        Title="{ll:Str '设置'}" 
        Height="170" Width="100"
        MinWidth="260" MinHeight="170"
        MaxWidth="300" MaxHeight="200"
        Closing="Window_Closing" Loaded="Window_Loaded"
        >
    <FrameworkElement.Resources>
        <ResourceDictionary Source="/VPet-Simulator.Windows.Interface;component/ResourceStyle.xaml" />
    </FrameworkElement.Resources>
    <Grid Background="{DynamicResource PrimaryLighter}" 
          Width="Auto" Height="Auto" 
          HorizontalAlignment="Center" VerticalAlignment="Center"
          >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="20" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>
        <pu:Switch x:Name="ShowHealthBar" Grid.Row="1" Grid.Column="1" Background="Transparent"
                BorderBrush="{DynamicResource PrimaryDark}" BoxHeight="18" BoxWidth="35"
                CheckedBackground="{DynamicResource Primary}"
                CheckedBorderBrush="{DynamicResource Primary}"
                CheckedToggleBrush="{DynamicResource DARKPrimaryText}" 
                ToggleBrush="{DynamicResource PrimaryDark}" ToggleShadowColor="{x:Null}"
                ToggleSize="14" 
                Content="{ll:Str '显示健康条'}" ToolTip="{ll:Str '在面板显示健康值状态条（需要重启桌宠以应用更改）'}"
                Checked="ShowHealthBar_CheckedChange" Unchecked="ShowHealthBar_CheckedChange" />
        <pu:Switch x:Name="ShowLikabilityBar" Grid.Row="2" Grid.Column="1" Background="Transparent"
                BorderBrush="{DynamicResource PrimaryDark}" BoxHeight="18" BoxWidth="35"
                CheckedBackground="{DynamicResource Primary}"
                CheckedBorderBrush="{DynamicResource Primary}"
                CheckedToggleBrush="{DynamicResource DARKPrimaryText}" 
                ToggleBrush="{DynamicResource PrimaryDark}" ToggleShadowColor="{x:Null}"
                ToggleSize="14" 
                Content="{ll:Str '显示好感条'}" ToolTip="{ll:Str '在面板显示好感值状态条（需要重启桌宠以应用更改）'}"
                Checked="ShowLikabilityBar_CheckedChange" Unchecked="ShowLikabilityBar_CheckedChange" />
        <Button x:Name="SaveButton" Grid.Row="4" Grid.Column="1" Height="30" HorizontalAlignment="Center"
                VerticalAlignment="Center" pu:ButtonHelper.CornerRadius="4"
                Background="{DynamicResource SecondaryLight}"
                BorderBrush="{DynamicResource SecondaryDark}" BorderThickness="2"
                Content="{ll:Str '保存'}" Click="SaveButton_Click"/>
        <!--win_setting_saveButton_content-->
    </Grid>
</Window>
